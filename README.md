# share-bridge-build

Public repository containing **GitHub Actions workflows only**.

- Source code lives in a separate **private** repo: `FuChuZhao/share-bridge`.
- Builds are manual-only (`workflow_dispatch`).
- Artifacts are short-lived and should be deleted after download.
